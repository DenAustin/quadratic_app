//Linear and Quadratic equations
//note f(x) is y

// Linear: y = mx+b; 
// f(x) = mx +b
// Linear produces a slope (Dy/Dx = 3/1)
// i.e, change in x produces predictable change in y


// Quadratic: y = ax**2 + bx + c
// f(x) = a(x**2) + bx + c ;
  //a(x**2) + bx + c  = 0;

// In quadratic function, change in x does not produce change in y. But it produces predictable change in change in y

// Exponential: y = Ar**x
//f(x) = a(b**x)

// In exponential function, changes in y follows a multiplier factor ;


//quadratic equation allmighty formular 
// eqn: -2(x**2) + || - Math.sqrt((b))

// const x = (-b +||- Math.sqrt((b**2)- 4(a*c)))/ (2*a)



